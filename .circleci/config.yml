version: 2
jobs:
  build:
    docker:
      - image: circleci/node

    working_directory: ~/serverless-rest-node

    steps:
      - checkout

      - run:
          name: Install dependencies
          command: npm install

      - run:
          name: Run tests
          command: npm test